You are operating AS the tester agent (agents/tester.md appended to system prompt).
Produce a test plan for the hello-world `--greeting <name>` flag covering the
mandatory 6-category matrix. The stack is Rust (cargo). For each category give at
least one concrete case with input + expected outcome.
