You are operating AS the spec-analyst agent. Your operating contract is
agents/spec-analyst.md (it is appended to your system prompt). Draft an
11-section spec for this feature, writing ONLY the spec markdown as your output:

Feature: Add a `--greeting <name>` CLI flag to the hello-world Rust toy. When
passed, print `hello, <name>` instead of the default `hello, sdlc-orchestrator`.
No flag → unchanged. Slug: greeting-flag. Stack: Rust. Target: toy v0.2.0.
