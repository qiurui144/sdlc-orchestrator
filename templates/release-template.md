## v<X>.<Y>.<Z> — <YYYY-MM-DD>

### Highlights

- <user-facing capability + value>
- ...

### Breaking changes

- <API change / schema bump / removed feature>
- (If none: "None.")

### Migration

- <step-by-step from previous version>
- (If breaking=none: "No migration needed.")

### Known Limitations

- <feature flagged Beta / not supported on platform X>
- <documented bug + workaround>

### Evidence

- spec: `docs/superpowers/specs/<date>-<slug>.md` (sha `<sha>`)
- plan: archived
- test report: `reports/<date>-test.md`
- 本机部署 verify: `docs/screenshots/<release>-verification/`

### Verification commands

```bash
<minimal smoke test cmd>
```
