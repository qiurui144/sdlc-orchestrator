# hello-world

Tiniest Rust project used as the sdlc-orchestrator end-to-end demo target.

See `SDLC_DEMO.md` for the walkthrough from `/sdlc:spec hello` through `/sdlc:release v0.1.0`.

## Run

```bash
cargo run
```

Expected output: `hello, sdlc-orchestrator`
